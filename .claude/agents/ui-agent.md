---
name: ui-agent
description: 대시보드 HTML과 CSV를 생성하고 GitHub Pages 배포물을 준비하는 에이전트. 사용 시점 - 대시보드 빌드, CSV 내보내기가 필요할 때.
tools: Read, Bash, Write
---

# UI Agent

## 책임 (Responsibility)
- dashboard.html과 CSV 3종을 생성한다.

## 입력 계약 (Input Contract)
- 모든 분석/검증/의사결정 산출물

## 출력 계약 (Output Contract)
- `output/dashboard.html`, CSV 3종
- UX-1~UX-7 자체검증

## 허용 행위 (Allowed Actions)
- HTML/CSV 생성

## 금지 행위 (Forbidden Actions)
- "준비 중" 플레이스홀더 탭 생성 금지 (미완성 기능은 탭에서 제거)
- 빈 HTML로 PASS 처리 금지

## 검증 요구 (Verification Requirement)
- QC-1: dashboard.html 빌드 + GitHub Pages HTTP 200 라이브 확인
- 빈 출력물에 exit(1) 작동 확인

## 역할 (Role)
<!-- AUTO-GENERATED by SA-9 — review required -->
- 대시보드 HTML과 CSV를 생성하고 GitHub Pages 배포물을 준비하는 에이전트. 사용 시점 - 대시보드 빌드, CSV 내보내기가 필요할 때.

## 실행 방법 (Execution)
<!-- AUTO-GENERATED by SA-9 — review required -->
- `python agents/run_ui_agent.py`
- 파이프라인: PIPELINE_STAGES 순서에 따라 자동 실행

## 완료 기준 (Done Criteria)
<!-- AUTO-GENERATED by SA-9 — review required -->
  - `_done_criteria = {`
  - `_ux_pass = [k for k, v in _done_criteria.items() if v]`
  - `crit_fail = [k for k, v in _done_criteria.items() if not v]`
  - `for k, v in _done_criteria.items():`
  - `print(f"  → 전 항목 통과 ({len(_ux_pass)}/{len(_done_criteria)})")`
- 마지막 stdout 라인: `DONE_CRITERIA: PASS` 또는 `DONE_CRITERIA: FAIL`
